Toggle the python-fp-lint PreToolUse gate on or off for the current project. Accepts an optional argument: `on` to explicitly enable, `off` to explicitly disable, `status` to check, or no argument to toggle.

The argument is: $ARGUMENTS

Run this exact command:

```bash
~/.claude/plugins/python-fp-lint/bin/lint $ARGUMENTS
```

Respond with exactly what the command prints. Do not explain. Do not ask for confirmation.
